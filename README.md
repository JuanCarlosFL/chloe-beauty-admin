# origin-front-admin

Instalamos las rutas
npm install react-router-dom --save
npm install @types/react-router-dom --save

Creamos las scenes y un barrel para exportarlos

Creamos src-->core-->router-->routes.ts
Creamos router.component.tsx
Creamos alias para scene y core
Creamos barrel para exportar

e importamos en app.tsx

instalamos material ui y emotion y lodash merge
npm install @material-ui/core @material-ui/icons --save
npm install @emotion/css --save
npm install lodash.merge --save

creamos el layouts --> centered.layout.ts y sus estilos

Instalamos formik y fonk
npm install formik --save
npm install @lemoncode/fonk @lemoncode/fonk-formik --save
