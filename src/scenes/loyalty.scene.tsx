import React from 'react';
import { LoyaltyContainer } from 'pods/loyalty';
import { AppLayout } from 'layout';

export const LoyaltyScene: React.FC = () => {
  return (
    <AppLayout>
      <LoyaltyContainer />
    </AppLayout>
  );
};
